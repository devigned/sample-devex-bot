apps:
  - name: sallybot
    script: npm run start
    exec_interpreter: sh
    args: "--adapter slack"
    watch:
      - package.json
        external-scripts.json
        scripts/
        hubot-scripts.json
        bin/hubot
  }]
}
